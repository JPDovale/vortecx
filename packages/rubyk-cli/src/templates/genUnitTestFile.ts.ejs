import { <%= props.name %> } from './<%= props.filename %>'

let sut: <%= props.name %>

describe('<%= props.name %>', async () => {
  beforeEach(async () => {
    sut = new <%= props.name %>()
  })

  it.skip('should ...', async () => {
    // Arrange
    // Act
    // Assert
  })
})