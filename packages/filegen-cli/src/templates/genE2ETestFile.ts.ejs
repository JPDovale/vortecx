import { <%= props.name %> } from './<%= props.filename %>'

let sut: <%= props.name %>

describe('<%= props.name %>', async () => {
  beforeAll(async () => {
    sut = new <%= props.name %>()
  })

  test.skip('E2E Execution', async () => {
    // Arrange
    // Act
    // Assert
  })
})